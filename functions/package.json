{
  "name": "functions",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions",
    "deploy:resetUserPassword": "npm run build && firebase deploy --only functions:resetUserPassword"
  },
  "main": "lib/index.js",
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.8.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "private": true
}
